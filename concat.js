///
//* concat method
// * if we want to use two array combine 
// * use FirstArrayName.concat(secondArrayname)
const var1 = [1, 2, 3, 8];
const var2 = [5, 88, 33, 48];
console.log(var1.concat(var2));
